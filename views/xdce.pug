extends layout

block content
  h4 XD Link URL
  form(action="/xd/entry" method="get")
    div(class="form-group")
      input(type="text" name="url" size="40")
      button(type="submit" class="btn btn-primary") Submit